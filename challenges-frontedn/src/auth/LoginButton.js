import React from "react";

const LoginButton= ()=>{
    const login = async()=> {

    };

    return(
        <button className="Login-button" onClick={()=> login()}>
            log in
        </button>
    );
};

export default LoginButton;