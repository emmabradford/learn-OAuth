import React from 'react';

const LogoutButton = ()=> {
    const logout = async ()=> {

    };

    return(
        <button className='Logout-buttin' onClick={()=> logout()}>
            logout
        </button>
    );
};

export default LogoutButton;